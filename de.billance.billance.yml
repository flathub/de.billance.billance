app-id: de.billance.billance
runtime: org.freedesktop.Platform
runtime-version: '25.08'
sdk: org.freedesktop.Sdk
base: org.electronjs.Electron2.BaseApp
base-version: '25.08'
command: billance
separate-locales: false

finish-args:
  - --share=ipc
  - --device=dri
  - --socket=x11
  - --share=network
  - --env=ELECTRON_TRASH=gio
  # Generated invoices default to the user's Documents folder (the app's
  # default output location) and are read back from there to display them
  # in-app. Any other output location is chosen by the user through the
  # file-chooser portal, so no broader home access is required.
  - --filesystem=xdg-documents

modules:
  - name: billance
    buildsystem: simple
    build-commands:
      - mkdir -p /app/main
      - cp -a app/. /app/main
      - patch-electron-desktop-filename /app/main/resources/app.asar --desktop-filename de.billance.billance.desktop
      - install -Dm755 billance-wrapper.sh /app/bin/billance
      - install -Dm644 app/de.billance.billance.desktop /app/share/applications/de.billance.billance.desktop
      - install -Dm644 app/de.billance.billance.metainfo.xml /app/share/metainfo/de.billance.billance.metainfo.xml
      - install -Dm644 app/de.billance.billance.svg /app/share/icons/hicolor/scalable/apps/de.billance.billance.svg
      - install -Dm644 LICENSE.proprietary.txt /app/share/licenses/de.billance.billance/LICENSE.proprietary.txt
    sources:
      - type: archive
        url: https://billance.de/downloads/flatpak/2026.1.0/Billance-2026.1.0-linux-x86_64-flatpak-extra-data.tar.gz
        sha256: 18d44800eafb1ffd758cd9325acffc3cb973a001555d242d557a44b6156b9e85
        dest: app
        strip-components: 0
        only-arches:
          - x86_64
      - type: archive
        url: https://billance.de/downloads/flatpak/2026.1.0/Billance-2026.1.0-linux-aarch64-flatpak-extra-data.tar.gz
        sha256: 4854cb3b918de8d5d5bb9b501fb3f9ff4d8a808252441b212bf72c01ba1c9345
        dest: app
        strip-components: 0
        only-arches:
          - aarch64
      - type: file
        path: billance-wrapper.sh
      - type: file
        path: LICENSE.proprietary.txt
